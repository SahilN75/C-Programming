#include <stdio.h>
int main() {
    char name[10];
    float salary;
    printf("Enter name of the employee: ");
    scanf("%s",&name);
    printf("Enter salary: ");
    scanf("%f",&salary);
    printf("%s",name);

    if (salary<=10000000) {
       tax = 0.05 * salary; 
    }else if (salary>10000000 && salary<=100000000) {
        
    }
   
}